# 已走过但不建议继续的方向

本文件记录已经尝试过、证据不足或证明无效的路线。它不是失败羞耻柱，而是避免重复消耗、保护负结果和明确重新考虑条件的研究资产。

当前暂无正式 wrong turn。初始化阶段不要把未尝试的想法提前判为失败。

## 0. 记录规则

- 先说明为什么值得尝试，再说明发生了什么。
- 区分“负结果”“证据不足”“实现失败”“评估无效”“report-only”和“高风险”。
- 绑定命令、config、日志、输出和指标；缺证据时标记 `待确认`。
- 写清何种新证据出现后可以重新考虑。
- 不要把一次运行失败等同于方法被证伪。
- 不要把不同 dataset / split / protocol 的结果直接比较后宣布路线失败。

## 方向名称

### 状态

负结果 / 证据不足 / 实现失败 / report-only / 不推荐主线 / 已归档

证据强度：high / medium / low / 待确认

### 为什么尝试

描述假设、预期机制和与 baseline 的关系。

### 发生了什么

记录实际运行、观察、指标和异常。不要只写主观结论。

### 为什么停止

说明是效果不足、风险过高、证据无效、资源不成比例，还是被更好的方向替代。

### 证据

- command / script：`待确认`
- config / seed：`待确认`
- output / log：`待确认`
- official evaluation：`待确认`

### 什么时候可以重新考虑

只有当 `待确认`。

### 推荐替代方向

- `待确认`

## 常见证据边界

- 不要把 simulation / report-only / matched-sample analysis 当正式系统收益。
- 不要把聊天建议、文件名或附件名当实验内容证据。
- 不要把不完整运行或损坏输出当正式结果。
- 不要只与过时 baseline 比；同时与 current best / accepted baseline 比。
- 不要在缺少 control、seed、scope 或 evaluation protocol 时下强结论。

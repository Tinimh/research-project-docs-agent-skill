# 文件地图

本文件用于快速定位 `{{PROJECT_NAME}}` 的长期入口。路径默认相对 `<PROJECT_ROOT>`；它是研究交接地图，不是所有文件的自动清单。

## 0. 使用约定

状态：`current / stable / historical / generated / archive / temporary / deprecated / 待确认`

可信度：`high / medium / low / 待确认`

## 1. 顶层目录

| 路径 | 类型 | 作用 | 状态 | 可信度 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `{{CODE_ROOT}}/` | code | 项目实现。 | current | medium | 具体入口待确认。 |
| `{{DATA_DIR}}/` | dataset | 数据和标签。 | current | medium | 默认只读；不要修改官方数据。 |
| `{{OUTPUT_DIR}}/` | outputs | 评估和生成产物。 | current | medium | 不覆盖历史结果。 |
| `{{LOG_DIR}}/` | logs | 运行日志。 | current | medium | 长运行写入。 |
| `experiments/` | experiment records | 实验级命令、配置、摘要和产物索引。 | current | high | 按日期和短名称建目录。 |
| `docs/` | documentation | 长期交接入口与专题文档。 | current | high | 根级保持简洁。 |

## 2. 根级交接文档

| 文件 | 类型 | 作用 | 状态 | 可信度 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `AGENTS.md` | rules | agent 工作流、证据、Git 和文档规则。 | stable | high | 新窗口先读。 |
| `docs/SCOPE.yml` | scope | 默认写入边界。 | stable | high | 编辑前先读。 |
| `docs/HANDOFF.md` | handoff | 当前状态、best/baseline、卡点和下一步。 | current | high | 核心入口。 |
| `docs/RESEARCH_LOG.md` | research log | 关键实验、指标、结论和决策。 | current | high | 不存原始大日志。 |
| `docs/COMMANDS.md` | commands | 长期可复用命令。 | current | high | 一次性命令迁出。 |
| `docs/FILE_MAP.md` | file map | 本文件。 | current | high | 只做索引。 |
| `docs/WRONG_TURNS.md` | negative log | 失败、证据不足和高风险路线。 | current | high | 避免重复投入。 |
| `docs/DOC_TEMPLATES.md` | templates | 可复制记录模板。 | stable | high | 写文档时使用。 |

## 3. 主入口

| 文件 | 类型 | 作用 | 状态 | 可信度 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `待确认` | entrypoint | 训练 / 推理 / 分析主入口。 | 待确认 | 待确认 | 用真实文件替换。 |

## 4. 核心实现文件

| 文件 / 目录 | 作用 | 状态 | 可信度 | 备注 |
| --- | --- | --- | --- | --- |
| `{{CODE_ROOT}}/` | 核心实现。 | current | medium | 按模块补充，不要枚举所有文件。 |

## 5. 关键脚本

| 脚本 | 作用 | 状态 | 可信度 | 备注 |
| --- | --- | --- | --- | --- |
| `待确认` | baseline / evaluation / report。 | 待确认 | 待确认 | |

## 6. 配置与环境

| 文件 | 作用 | 状态 | 可信度 | 备注 |
| --- | --- | --- | --- | --- |
| `待确认` | environment / dataset / model / eval config。 | 待确认 | 待确认 | |

## 7. 数据、输出与日志

| 路径 | 作用 | 状态 | 可信度 | 备注 |
| --- | --- | --- | --- | --- |
| `{{DATA_DIR}}/` | 数据集。 | current | medium | 默认只读。 |
| `{{OUTPUT_DIR}}/` | 正式或诊断输出。 | current | medium | 区分 official 与 report-only。 |
| `{{LOG_DIR}}/` | 运行日志。 | current | medium | 记录对应 experiment。 |

## 8. 当前主线文档

- `docs/HANDOFF.md`
- 其他专题入口：`待确认`

## 9. 历史路线与归档入口

- 暂无。归档后保留旧路径说明、状态、证据边界和新入口链接。
